{
  "name": "siddhi-green-website-root",
  "version": "1.0.0",
  "description": "Root configuration for Siddhi Green Website",
  "scripts": {
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && uvicorn server:app --reload",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
